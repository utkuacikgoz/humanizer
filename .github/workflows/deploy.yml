name: Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CF_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CF_D1_ID: ${{ secrets.CLOUDFLARE_D1_DATABASE_ID }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22.13.0"
          cache: npm

      - run: npm ci

      # vinext generates dist/server/wrangler.json from vite.config.ts,
      # baking in D1_DATABASE_ID (see vite.config.ts).
      - run: npm run build
        env:
          D1_DATABASE_ID: ${{ env.CF_D1_ID }}

      # Manual-trigger only (no push/merge auto-deploy): this repo's deploy
      # target is a project_id-tied hosting platform (.openai/hosting.json)
      # whose own deploy mechanism is unconfirmed. Auto-deploying here could
      # double-fire against it.
      - name: Deploy via wrangler
        if: ${{ env.CF_API_TOKEN != '' && env.CF_ACCOUNT_ID != '' && env.CF_D1_ID != '' }}
        run: npx wrangler deploy --config dist/server/wrangler.json
        env:
          CLOUDFLARE_API_TOKEN: ${{ env.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ env.CF_ACCOUNT_ID }}

      - name: Not configured
        if: ${{ env.CF_API_TOKEN == '' || env.CF_ACCOUNT_ID == '' || env.CF_D1_ID == '' }}
        run: |
          echo "::error::Set CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, and CLOUDFLARE_D1_DATABASE_ID repo secrets before running this workflow."
          exit 1
